@model IEnumerable<MVCDemo.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Ten sinh vien
        </th>
        <th>
           Tuoi sinh vien
        </th>
        <th></th>
    </tr>
@foreach (var item in (IEnumerable<MVCDemo.Models.Student>)ViewBag.students) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.age)
        </td>
        <td>
            <a href="/Student/Edit/2">Sua</a>|
            @Html.ActionLink("Chi tiet", "Details", new { id=item.id }) |
            @Html.ActionLink("Xoa", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
